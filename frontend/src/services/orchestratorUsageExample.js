/**
 * Example Usage of ArchiTechOrchestrator
 */

/*
import { ArchiTechOrchestrator } from './architechOrchestrator.js';

// Initialize
const orchestrator = new ArchiTechOrchestrator('YOUR_GEMINI_API_KEY');

// Register lifecycle callbacks
orchestrator.registerCallbacks({
  onLog: (msg) => console.log('[LOG]', msg),
  onPhaseChange: (phaseName, phaseNum) => {
    console.log(`[PHASE ${phaseNum}]`, phaseName);
  },
  onError: (err) => console.error('[ERROR]', err),
  onComplete: (result) => {
    console.log('[COMPLETED]', result);
    // Use result.architecture, result.devops
  }
});

// Run the orchestrator
await orchestrator.run('Build a Python FastAPI app with Redis');
*/
